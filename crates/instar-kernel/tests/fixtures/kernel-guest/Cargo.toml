[package]
name = "kernel-guest"
version = "0.0.0"
edition = "2021"
license = "MPL-2.0"
publish = false
description = "WP4 lifecycle guest fixture. Not part of the Instar workspace: targets wasm32-wasip2, built on demand by instar-kernel's build script."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.60.0"

[workspace]
